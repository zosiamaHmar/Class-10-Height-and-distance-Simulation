node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Claude Code / agent skills installed via `npx remotion skills add`.
# Regenerate with that command rather than committing them.
.agents
.claude
skills-lock.json
